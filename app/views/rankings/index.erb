<h1>Rankings</h1>
<hr>

<% @rankings.each do |ranking| %>
    <ol>
        <% ranking.players.each do |player| %>
            <li><%= player.name %></li>
        <% end %>

        <h5>Honorable Mention:</h5>
        <p><%= ranking.description %></p>
        <h6><%= ranking.created_at %></h6>
    <hr>
    </ol>
<% end %>
